# Skill 存储路径整合 - 自测文档

## 测试环境

- [ ] 开发环境已启动 (`pnpm dev`)
- [ ] utools 插件已加载
- [ ] 浏览器开发者工具已打开

## 功能测试

### 1. 页面访问测试

| 测试项 | 操作步骤 | 预期结果 | 实际结果 |
|--------|----------|----------|----------|
| 1.1 访问存储设置页面 | 点击导航栏 → 设置 → 存储 | 页面正常加载，显示存储设置 | |
| 1.2 页面布局 | 查看页面各区域 | 依次显示：SkillNest、存储路径、配置路径、同步方式、项目目标 | |

### 2. Agent 存储路径测试

| 测试项 | 操作步骤 | 预期结果 | 实际结果 |
|--------|----------|----------|----------|
| 2.1 查看默认路径 | 打开存储设置页面 | 显示各 agent 的存储路径或"未设置" | |
| 2.2 编辑路径 | 点击 Claude 的"编辑"按钮 → 输入新路径 → 点击确认 | 路径更新成功，显示新路径 | |
| 2.3 重置默认 | 编辑路径 → 点击"重置默认" | 恢复为默认路径 | |
| 2.4 取消编辑 | 编辑路径 → 按 Esc 键 | 取消编辑，保持原路径 | |
| 2.5 批量设置 | 依次设置所有 agent 的路径 | 所有路径设置成功 | |

### 3. 🆕 安装目录跟随配置测试

| 测试项 | 操作步骤 | 预期结果 | 实际结果 |
|--------|----------|----------|----------|
| 3.1 查看当前安装目录 | 打开 Skill 管理页面，查看安装位置 | 显示配置的 agent 目录，而非硬编码路径 | |
| 3.2 修改配置后生效 | 在存储设置中修改 Claude 路径 → 安装新 skill | skill 安装到新配置的路径 | |
| 3.3 多 agent 选择 | 配置多个 agent 路径 → 安装 skill | 可选择安装到哪个 agent 目录 | |
| 3.4 未配置时的默认行为 | 清空所有 agent 路径配置 → 安装 skill | 使用默认路径或提示配置 | |
| 3.5 重启后保持 | 修改安装目录 → 重启插件 | 安装目录配置保持不变 | |

### 4. Agent 配置路径测试（MCP 配置文件）

| 测试项 | 操作步骤 | 预期结果 | 实际结果 |
|--------|----------|----------|----------|
| 4.1 查看配置路径区域 | 滚动到"Agent 配置路径"区域 | 显示所有 agent 的配置路径设置 | |
| 4.2 设置 Claude 配置路径 | 点击编辑 → 输入 `~/.claude` → 确认 | 路径保存成功 | |
| 4.3 设置 Codex 配置路径 | 点击编辑 → 输入 `~/.codex` → 确认 | 路径保存成功 | |
| 4.4 设置 Gemini 配置路径 | 点击编辑 → 输入 `~/.gemini` → 确认 | 路径保存成功 | |
| 4.5 设置 OpenClaw 配置路径 | 点击编辑 → 输入 `~/.openclaw` → 确认 | 路径保存成功 | |
| 4.6 重置配置路径 | 编辑 → 点击"重置默认" | 恢复为默认配置路径 | |
| 4.7 MCP 使用配置路径 | 修改 Claude 配置路径 → 打开 MCP 管理 | MCP 从新路径读取配置 | |

### 5. Agent 会话路径测试（新增功能）

| 测试项 | 操作步骤 | 预期结果 | 实际结果 |
|--------|----------|----------|----------|
| 5.1 查看会话路径区域 | 滚动到"Agent 会话路径"区域 | 显示所有 agent 的会话路径设置 | |
| 5.2 设置 Claude 会话路径 | 点击编辑 → 输入 `~/.claude/projects` → 确认 | 路径保存成功 | |
| 5.3 设置 Codex 会话路径 | 点击编辑 → 输入 `~/.codex/sessions` → 确认 | 路径保存成功 | |
| 5.4 设置 OpenClaw 会话路径 | 点击编辑 → 输入 `~/.openclaw/sessions` → 确认 | 路径保存成功 | |
| 5.5 重置会话路径 | 编辑 → 点击"重置默认" | 恢复为默认会话路径 | |
| 5.6 会话使用配置路径 | 修改 Claude 会话路径 → 打开会话管理 | 从新路径扫描会话 | |
| 5.7 统计使用配置路径 | 修改 Claude 会话路径 → 打开用量统计 | 从新路径扫描统计数据 | |
| 5.8 Provider切换使用配置路径 | 修改 Claude 配置路径 → 切换 Provider | 配置写入新路径 | |
| 5.9 提示词文件使用配置路径 | 修改 Claude 配置路径 → 打开提示词管理 | 从新路径读取提示词 | |

### 6. 同步方式测试

| 测试项 | 操作步骤 | 预期结果 | 实际结果 |
|--------|----------|----------|----------|
| 6.1 选择软链接 | 点击"软链接"选项 | 选项被选中，显示高亮 | |
| 6.2 选择复制同步 | 点击"复制同步"选项 | 选项被选中，显示高亮 | |
| 6.3 切换同步方式 | 先选软链接，再选复制同步 | 切换成功，状态保持 | |

### 7. 项目同步目标测试

| 测试项 | 操作步骤 | 预期结果 | 实际结果 |
|--------|----------|----------|----------|
| 7.1 添加项目 | 输入名称和路径 → 点击"添加" | 项目添加成功，显示在列表中 | |
| 7.2 添加项目（回车） | 输入名称和路径 → 按回车 | 项目添加成功 | |
| 7.3 移除项目 | 点击项目右侧"移除" → 确认 | 项目移除成功 | |
| 7.4 空路径添加 | 不输入路径直接点击添加 | 无反应或提示错误 | |

### 8. 数据持久化测试

| 测试项 | 操作步骤 | 预期结果 | 实际结果 |
|--------|----------|----------|----------|
| 8.1 重启后保持 | 设置所有路径 → 关闭页面 → 重新打开 | 所有设置保持不变 | |
| 8.2 刷新页面 | 设置路径 → 刷新浏览器 | 设置保持不变 | |

### 9. UI 交互测试

| 测试项 | 操作步骤 | 预期结果 | 实际结果 |
|--------|----------|----------|----------|
| 9.1 响应式布局 | 调整浏览器窗口大小 | 布局正常，无溢出 | |
| 9.2 深色模式 | 切换到深色模式 | 样式正常，文字可读 | |
| 9.3 浅色模式 | 切换到浅色模式 | 样式正常，文字可读 | |
| 9.4 悬停效果 | 鼠标悬停在可点击元素上 | 显示悬停效果 | |

### 10. 错误处理测试

| 测试项 | 操作步骤 | 预期结果 | 实际结果 |
|--------|----------|----------|----------|
| 10.1 无效路径 | 输入不存在的路径 | 保存成功（路径验证由后端处理） | |
| 10.2 特殊字符路径 | 输入包含特殊字符的路径 | 保存成功或给出提示 | |
| 10.3 超长路径 | 输入超长路径字符串 | 正常处理，无界面异常 | |

## 边界情况测试

| 测试项 | 操作步骤 | 预期结果 | 实际结果 |
|--------|----------|----------|----------|
| 11.1 空配置 | 首次打开，无任何配置 | 显示默认状态，无报错 | |
| 11.2 部分配置 | 只设置部分 agent 的路径 | 未设置的显示"未设置"，已设置的正常显示 | |
| 11.3 快速操作 | 快速连续点击编辑/确认 | 无界面卡顿或数据错误 | |

## 性能测试

| 测试项 | 操作步骤 | 预期结果 | 实际结果 |
|--------|----------|----------|----------|
| 12.1 页面加载时间 | 打开存储设置页面 | 1秒内加载完成 | |
| 12.2 保存响应时间 | 点击确认保存路径 | 500ms内响应 | |

## 兼容性测试

| 测试项 | 操作步骤 | 预期结果 | 实际结果 |
|--------|----------|----------|----------|
| 13.1 utools 环境 | 在 utools 中运行插件 | 功能正常 | |
| 13.2 浏览器环境 | 在浏览器中访问（开发模式） | 功能正常（API 调用可能失败） | |

## 测试结果汇总

- 通过: ___/51
- 失败: ___/51
- 跳过: ___/51

## 发现的问题

| 问题编号 | 问题描述 | 严重程度 | 状态 |
|----------|----------|----------|------|
| | | | |

## 测试备注

- 测试日期: ___________
- 测试人员: ___________
- 测试版本: ___________
